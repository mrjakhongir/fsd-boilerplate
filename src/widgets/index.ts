// Widgets are composed UI blocks that combine multiple entities and/or features into reusable sections of a page.
// They are bigger than entities/ui components but smaller than full pages.

// Think of them as:
// "Smart UI blocks that assemble entities + features into meaningful sections."

// ✅ When to Create a Widget?

// ✅ Is this UI block reused across multiple pages?
// ✅ Does it combine more than one entity/feature?
// ✅ Is it too big to be just a component, but too small to be a page?
