// No business logic (doesn't know about users, products, etc.)
// Highly reusable across the entire app
// Props-driven behavior
// Styled with SCSS modules
// Can be moved to any project

export { CustomInput } from "./input";
