// The pages/ folder contains fully assembled screens that represent route-level units —
// meaning each file/folder in pages/ corresponds to a separate URL / route in the application.

// Pages = Final Composition Layer
// They assemble widgets + features + entities into a cohesive layout per route.
