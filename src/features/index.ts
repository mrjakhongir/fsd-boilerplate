// The features/ folder contains user-facing actions or capabilities — things a user can do in the system.

// If entities represent data (User, Product, Order), then features represent interactions with that data
// (Login, Add to Cart, Like Post, Apply Coupon).
